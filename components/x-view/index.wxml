<view class='item display {{target?"act":""}} item-{{row}} col-{{col}} bg-{{bg}} text-{{color}} border-{{radius}} {{shadow?"shadow":""}}' bindtap='_bindTap' style='height:{{height}};width:{{width}};background:{{bgStyle}};color:{{colorStyle}};font-size:{{font}};padding:{{padding}};margin:{{margin}};'>
  <slot></slot>
</view>
